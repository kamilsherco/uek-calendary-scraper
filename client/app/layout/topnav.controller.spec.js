'use strict';

describe('myApp.topnav module', function() {

  beforeEach(module('myApp.topnav'));

  describe('myApp.topnav controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var topNavCtrl = $controller('TopNavCtrl');
      expect(topNavCtrl).toBeDefined();
    }));

  });
});
